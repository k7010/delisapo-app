<!DOCTYPE html>
<html>
  <head>
    <title>DelisapoApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/earlyaccess/nicomoji.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="http://yui.yahooapis.com/3.18.1/build/cssreset/cssreset-min.css">
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="a">
      <div class="header">
        <div class="header-logo">
          <%= link_to 'デリサポ', "/", class: "header-title"%>
        </div>
        <div class="header-menu">
          <% if user_signed_in? %>
            <div class="user-logout">
              <span class="staff-number"><%= current_user.staff_number %></span>
              <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "logout" %>
            </div>
          <% else %>
            <div class="user-sign">
              <%= link_to "Sign In", new_user_session_path, class: "login" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "sign-up" %>
            </div>
          <% end %>
        </div>
      </div>
    </header>
    <div>
    <div>
      <%= yield %>
      </div>
    </div>
    <footer>
      <div class="footer">
        <p>© 2021 Kawasaki</p>
      </div>
    </footer>
  </body>
</html>
