<h1>トップページ</h1>
<%= link_to "持出登録", new_baggage_path %><br>

<% if user_signed_in? %>
<h1>持出一覧</h1>

<%# 一覧表示 %>
<% @baggages.each do |baggage| %>
  <%= link_to baggage_path(baggage.id), method: :get do %>
    <%= render partial: "baggage", locals: { baggage: baggage } %>
  <% end %>
    <% @results.where(id: baggage.id).each do %>
      <p style="display: inline">完了</p>
    <% end %><br>
<% end %>

    
  
<h1>配達実績を調べる</h1>
<%= render 'search_form', url: baggages_path, q: @q %>

<% else %>
<%# ログアウト状態は以下を表示 %>
  <h1>今日も一日がんばりましょう</h1>
  <h1>新規登録・ログインして業務を開始して下さい</h1>
<% end%>